/*************************
 * 邮件内容处理服务
 */
module.exports = {
    parse: function(content, callBack) {
        // ETemplateVariable.find({ status: 1 }).then(function(variables) {
        //     (variables).forEach(function(item) {
        //         eval("var reg=/" + item.code + "/g");
        //         content = content.replace(reg, )
        //     }, this);
        // });
    }
}